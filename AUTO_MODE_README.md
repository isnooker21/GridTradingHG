# 🤖 Full Auto Trading System - User Guide

## ✅ สิ่งที่ได้พัฒนาเสร็จแล้ว

### 📁 ไฟล์ใหม่ที่สร้าง (3 ไฟล์)

#### 1. **atr_calculator.py**
- คำนวณ ATR (Average True Range) period 14, Timeframe M15
- มี cache 60 วินาที เพื่อลดการเรียก MT5
- ฟังก์ชัน:
  - `calculate_atr()` → คืนค่า ATR ในหน่วย pips
  - `get_volatility_level()` → คืนค่า "LOW", "MODERATE", "HIGH", "VERY HIGH"
  - `get_atr_info()` → คืน Dict พร้อมข้อมูลทั้งหมด

#### 2. **trend_detector.py**
- ตรวจจับ Trend ด้วย EMA Crossover (EMA 20 และ EMA 50)
- ใช้ Timeframe M15
- ฟังก์ชัน:
  - `calculate_ema(period)` → คืนค่า EMA
  - `detect_trend()` → คืน "buy", "sell", "both"
  - `get_trend_strength()` → คืน "strong", "weak", "neutral"
  - `get_trend_info()` → คืน Dict พร้อมข้อมูลทั้งหมด

**Trend Detection Logic:**
- **UPTREND + Price ABOVE** → "buy"
- **DOWNTREND + Price BELOW** → "sell"
- **SIDEWAYS** → "both"

#### 3. **auto_config_manager.py**
- คำนวณค่า Config อัตโนมัติจาก ATR และ Trend
- รองรับ 5 Risk Profiles:
  - ⭐ Very Conservative
  - ⭐⭐ Conservative
  - ⭐⭐⭐ Moderate (แนะนำ)
  - ⭐⭐⭐⭐ Aggressive
  - ⭐⭐⭐⭐⭐ Very Aggressive
- ฟังก์ชัน:
  - `calculate_auto_settings(risk_profile)` → คำนวณค่าทั้งหมด
  - `get_risk_profile_multipliers(profile)` → คืน multipliers
  - `calculate_survivability(balance, price, leverage, settings)` → คำนวณว่าทนได้กี่ pips

### 🔧 ไฟล์ที่แก้ไข (3 ไฟล์)

#### 1. **config.py**
- เพิ่ม `auto_mode: bool` → เปิด/ปิด Auto Mode
- เพิ่ม `risk_profile: str` → เลือก Risk Profile
- เพิ่ม `last_auto_update: datetime` → บันทึกเวลาอัพเดทล่าสุด
- เพิ่ม comment สำหรับ `hg_multiplier` → ใช้ในการคำนวณ HG Lot

#### 2. **grid_manager.py**
- เพิ่มฟังก์ชัน `check_and_update_auto_settings()` → อัพเดทค่าทุก 15 นาที
- แก้ไข `update_grid_status()` → เรียกใช้ auto settings ถ้าเปิด Auto Mode

#### 3. **gui.py**
- เพิ่ม Mode Selection: 📝 Manual Mode และ 🤖 Full Auto Mode
- เพิ่ม Auto Mode Display ที่แสดง:
  - 📈 Market Analysis: Trend, ATR, EMA(20), EMA(50)
  - ⚙️ Auto Calculated: Grid Distance, HG Distance, Direction
  - 🛡️ Risk Profile Selection
  - 📊 Survivability Analysis
- เพิ่มฟังก์ชัน:
  - `toggle_mode()` → สลับระหว่าง Manual/Auto
  - `refresh_auto_analysis()` → รีเฟรชข้อมูล Auto Mode
  - `display_survivability()` → แสดง Survivability Analysis
  - `create_auto_mode_ui()` → สร้าง UI สำหรับ Auto Mode

---

## 🎮 วิธีการใช้งาน

### 1. เปิดใช้งาน Auto Mode

1. เปิดโปรแกรม Grid Trading System
2. เชื่อมต่อกับ MT5 (กดปุ่ม "Connect MT5")
3. เลือก **🤖 Full Auto Mode** จาก Mode Selection
4. เลือก Risk Profile ที่เหมาะสม (แนะนำ: ⭐⭐⭐ Moderate)
5. กดปุ่ม "🔄 Refresh Analysis" เพื่อดูข้อมูล
6. กดปุ่ม "💾 Save Settings" เพื่อบันทึกการตั้งค่า
7. กดปุ่ม "▶ Start Trading" เพื่อเริ่มเทรด

### 2. ตั้งค่า Lot Size และ HG Multiplier

**สิ่งสำคัญ:** แม้จะใช้ Auto Mode แต่ Lot Size และ HG Multiplier ยังต้องตั้งค่าเอง

1. ไปที่ **Manual Mode** ชั่วคราว
2. ตั้งค่า:
   - **Grid Lot Size**: เช่น 0.01, 0.02, 0.05
   - **HG Initial Lot**: เช่น 0.01, 0.02
   - **HG Multiplier**: เช่น 1.2, 1.5 (ยิ่งสูง = HG Lot ยิ่งใหญ่)
3. กดปุ่ม "💾 Save Settings"
4. กลับไปที่ **Auto Mode**

### 3. อ่านผล Survivability Analysis

ระบบจะแสดงว่าบัญชีของคุณทนได้กี่ pips ในกรณีที่เลวร้ายที่สุด (ราคาเดินทางเดียวไม่กลับ)

```
🎯 GRID + HG SYSTEM:
   Max Distance:     1,500 pips    <- ทนได้กี่ pips
   Max Grid Levels:  30 levels     <- วางไม้ได้กี่ level
   Max HG Levels:    5 levels      <- ออก HG ได้กี่ครั้ง
   Max Margin Used:  $2,500.00     <- ใช้ Margin สูงสุด
   Max Drawdown:     -$1,200.00    <- ขาดทุนสูงสุด
   Final Equity:     $3,800.00     <- Equity เหลือ
   Margin Level:     152.0%        <- Margin Level
```

**แนะนำ:** ให้เหลือ Buffer อย่างน้อย 30-50%

---

## ⚙️ การทำงานของ Auto Mode

### 📊 ทุก 15 นาที
- คำนวณ ATR ใหม่ (period 14, M15)
- ตรวจจับ Trend ใหม่ (EMA 20/50, M15)
- คำนวณค่า Grid Distance, HG Distance อัตโนมัติ
- อัพเดท Direction ตาม Trend:
  - **Uptrend** → เปิดแค่ Buy
  - **Downtrend** → เปิดแค่ Sell
  - **Sideways** → เปิดทั้ง Buy และ Sell

### 📈 Auto Calculation Formula

```
ATR = Average True Range (period 14, M15)
Grid Distance = ATR × grid_atr_multiplier
HG Distance = Grid Distance × hg_grid_multiplier
HG SL Trigger = HG Distance × hg_sl_ratio
Direction = Trend (buy/sell/both)
```

**ตัวอย่าง** (Risk Profile: Moderate)
- ATR = 80 pips
- Grid Distance = 80 × 1.0 = 80 pips
- HG Distance = 80 × 3.0 = 240 pips
- HG SL Trigger = 240 × 0.5 = 120 pips

---

## 🛡️ Risk Profiles

### ⭐ Very Conservative
- Grid กว้างมาก (ATR × 2.0)
- HG ไกลมาก (Grid × 5.0)
- เหมาะสำหรับ: บัญชีเล็ก, ไม่ชอบความเสี่ยง

### ⭐⭐ Conservative
- Grid กว้าง (ATR × 1.5)
- HG ไกล (Grid × 4.0)
- เหมาะสำหรับ: บัญชีปานกลาง, ต้องการความปลอดภัย

### ⭐⭐⭐ Moderate (แนะนำ)
- Grid สมดุล (ATR × 1.0)
- HG ปานกลาง (Grid × 3.0)
- เหมาะสำหรับ: ส่วนใหญ่, สมดุลระหว่างกำไรและความเสี่ยง

### ⭐⭐⭐⭐ Aggressive
- Grid แคบ (ATR × 0.7)
- HG ใกล้ (Grid × 2.5)
- เหมาะสำหรับ: บัญชีใหญ่, ชอบความเสี่ยง

### ⭐⭐⭐⭐⭐ Very Aggressive
- Grid แคบมาก (ATR × 0.5)
- HG ใกล้มาก (Grid × 2.0)
- เหมาะสำหรับ: ผู้เชี่ยวชาญ, รับความเสี่ยงสูง

---

## ⚠️ คำเตือนและข้อควรระวัง

### 1. Safety Limits
- Grid Distance: 20-200 pips
- HG Distance: 100-1000 pips
- ถ้าค่าเกินขีด ระบบจะปรับให้อัตโนมัติ

### 2. Worst Case Scenario
- การคำนวณ Survivability ใช้กรณีเลวร้ายที่สุด
- **ไม่รวม:** Spread, Commission, Swap
- **ไม่รวม:** กำไรจาก TP

### 3. Buffer Recommendation
- แนะนำให้เหลือ Buffer 30-50%
- ตัวอย่าง: ถ้าทนได้ 1,500 pips → ให้ระวังเมื่อราคาเดินทางไป 1,000 pips

### 4. Market Conditions
- ตลาดผันผวนมาก → ATR สูง → Grid กว้างขึ้น
- ตลาดเงียบ → ATR ต่ำ → Grid แคบลง
- ระบบจะปรับอัตโนมัติทุก 15 นาที

### 5. Lot Size Management
- เริ่มต้นด้วย Lot Size เล็กๆ (0.01)
- ทดสอบดูก่อนว่าระบบทำงานถูกต้อง
- ค่อยๆ เพิ่ม Lot Size เมื่อมั่นใจแล้ว

---

## 🔍 การตรวจสอบและ Debug

### ตรวจสอบว่า Auto Mode ทำงานหรือไม่

1. ดูที่ **Activity Log**:
```
[12:00:00] ✓ Switched to Full Auto Mode
[12:00:05] 🔄 Auto Mode: Updating settings...
[12:00:05] ✓ Auto settings updated: Grid=80pips, HG=240pips, Direction=both
```

2. ดูที่ **Auto Mode Status**:
- Last Update: ต้องแสดงเวลาล่าสุด
- Next: ต้องแสดงเวลาอัพเดทครั้งถัดไป (15 นาที)

3. ดูที่ **Log File** (`trading_bot.log`):
```
INFO - ATR calculated: 80.5 pips (period 14, TF M15)
INFO - Trend: BUY (Uptrend + Above) - EMA_Fast: 2650.25, EMA_Slow: 2640.10
INFO - Auto settings calculated: Grid=80pips, HG=240pips, Direction=buy
```

### ปัญหาที่อาจพบและวิธีแก้

#### 1. "Cannot calculate ATR"
- **สาเหตุ:** MT5 ไม่เชื่อมต่อ หรือไม่มีข้อมูลแท่งเทียน M15
- **แก้ไข:** 
  - เช็คว่า MT5 เชื่อมต่ออยู่
  - เปิด Chart M15 ของ XAUUSD ใน MT5
  - รอให้มีข้อมูลพอ (อย่างน้อย 50 แท่ง)

#### 2. "Cannot detect trend"
- **สาเหตุ:** คล้ายกับข้างบน
- **แก้ไข:** เหมือนกับข้างบน

#### 3. Survivability แสดงผลผิดพลาด
- **สาเหตุ:** ค่า Lot Size หรือ HG Multiplier ไม่ถูกต้อง
- **แก้ไข:**
  - ไปที่ Manual Mode
  - ตรวจสอบและแก้ไขค่า Lot Size, HG Multiplier
  - Save Settings แล้วกลับไป Auto Mode

---

## 📝 สรุป

### ✅ ข้อดีของ Auto Mode
- ✅ ปรับค่าอัตโนมัติตามความผันผวนของตลาด
- ✅ ตรวจจับ Trend และปรับ Direction อัตโนมัติ
- ✅ ประหยัดเวลา ไม่ต้องปรับค่าเอง
- ✅ มี 5 Risk Profiles ให้เลือก
- ✅ แสดง Survivability Analysis แบบ Real-time

### ⚠️ สิ่งที่ต้องตั้งค่าเอง
- ⚠️ Lot Size (Grid และ HG)
- ⚠️ HG Multiplier
- ⚠️ Risk Profile

### 🎯 แนะนำสำหรับมือใหม่
1. เริ่มต้นด้วย Risk Profile: **Moderate**
2. ใช้ Lot Size เล็กๆ: **0.01**
3. ตรวจสอบ Survivability ให้เหลือ Buffer อย่างน้อย **50%**
4. ทดสอบระบบก่อนใช้งานจริง (Demo Account)
5. เฝ้าดูระบบอย่างใกล้ชิดในช่วงแรก

---

**สร้างโดย:** AI Assistant
**วันที่:** 2025-11-05
**เวอร์ชัน:** 1.0

หากมีปัญหาหรือต้องการความช่วยเหลือเพิ่มเติม กรุณาติดต่อผู้พัฒนา

